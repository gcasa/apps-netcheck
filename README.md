# NetCheck

NetCheck is a small GNUstep dock monitor for Linux network interfaces.

It reads `/proc/net/dev`, polls every two seconds, and updates the dock icon with compact per-interface receive/transmit activity bars.

Build with:

```sh
make
```
